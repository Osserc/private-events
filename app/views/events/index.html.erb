<div class="events-index">
  <div class="current-events-bloc">
    <div class="events-header white">Current events</div>
  
    <div class="current-events">
      <% a = 1; Event.current.each do |event| %>
        <% unless event.private == true && !check_right_user(event) %>
          <div class="single-event <%= a.odd? ? "green" : "white" %>">
            <%= render "event_preview", :event => event %>
          </div>
          <% a += 1%>
        <% end %>
      <% end %>
    </div>
  </div>
  
  <div class="past-events-bloc">
    <div class="events-header white">Past events</div>
  
    <div class="past-events">
      <% a = 1; Event.past.each do |event| %>
        <% unless event.private == true && !check_right_user(event) %>
          <div class="single-event <%= a.odd? ? "green" : "white" %>">
            <%= render "event_preview", :event => event %>
          </div>
          <% a += 1%>
        <% end %>
      <% end %>
    </div>
  </div>
</div>